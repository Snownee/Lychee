package snownee.lychee.util.context;

import snownee.lychee.util.SerializableType;

/**
 * The context that need save to the world.
 */
public interface SerializableContextType<T extends LycheeContextValue<T>> extends SerializableType<T> {
}
